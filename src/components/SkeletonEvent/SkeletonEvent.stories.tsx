import { storiesOf } from '@storybook/react';
import * as React from 'react';

import SkeletonEvent from './SkeletonEvent';

storiesOf('SkeletonEvent', module).add('default', () => <SkeletonEvent />);
